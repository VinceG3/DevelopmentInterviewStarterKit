json.metadata {
  json.perPage @per_page
  json.currentPage @current_page
  json.nextPage @next_page
  json.prevPage @prev_page
  json.totalPages @total_pages
  json.totalCount @total_count
}
json.people @people
